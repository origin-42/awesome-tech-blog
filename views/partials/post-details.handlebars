<div id="comment{{id}}">
  <div class="container">
    <div class="row">
      <div class="col-sm">
        <p>{{comment}}</p>
      </div>
      <div class="col-sm">
        <p>{{user.username}} {{createdAt}}</p>
      </div>
    </div>
  </div>
</div>